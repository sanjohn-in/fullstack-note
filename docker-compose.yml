services:

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: Techbodia@2026
      MYSQL_DATABASE: techbodia
    ports:
      - "3307:3306"
    restart: always

  backend:
    build: ./backend
    ports:
      - "5239:9000"
    depends_on:
      - mysql
    restart: always
